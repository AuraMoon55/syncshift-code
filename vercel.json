{
    "installCommand": "yarn install-dep",
    "buildCommand": "yarn build",
    "outputDirectory": ".",
    "routes":[
        {
            "src":"/(.*)",
            "dest":"index.js"
        }
    ],
    {
  "rewrites": [{ "source": "/(.*)", "destination": "index.js" }]
}
}
